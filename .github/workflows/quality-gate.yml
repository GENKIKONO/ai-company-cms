name: 🚦 Quality Gate

# シンプルな品質ゲート（すべてのPRで実行）
on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main]

env:
  NODE_VERSION: '20'

concurrency:
  group: quality-gate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gate:
    name: 🚦 Quality Gate
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📲 Install dependencies
        run: npm ci

      - name: 🎨 Tailwind direct write check
        run: npm run check:tailwind

      - name: 🔤 TypeScript (strict)
        run: npm run typecheck

      - name: 🧹 ESLint (no warnings)
        run: npm run lint -- --max-warnings 0

      - name: 🔐 API Auth Pattern Check (info only until Gate v3)
        continue-on-error: true  # Gate v3 で削除してエラー強制化
        run: |
          OUTPUT=$(npm run check:api-auth 2>&1)
          echo "$OUTPUT"
          ERROR_COUNT=$(echo "$OUTPUT" | grep -oP '\d+(?= error\(s\))' | head -1 || echo "0")
          if [ "$ERROR_COUNT" != "0" ] && [ -n "$ERROR_COUNT" ]; then
            echo "⚠️ API Auth violations: $ERROR_COUNT error(s) (not blocking until Gate v3)"
          else
            echo "✅ API Auth check passed"
          fi

      - name: 🌐 Origin Safety Check
        run: npm run check:origin-safety

      - name: 🔥 App Route Smoke Test (v1.1)
        run: npm run check:app-routes
        env:
          SMOKE_TEST_URL: ${{ secrets.SMOKE_TEST_URL || '' }}

      - name: 🏗️ Build (no warnings)
        run: |
          BUILD_OUTPUT=$(npm run build 2>&1) || exit 1
          WARNING_COUNT=$(echo "$BUILD_OUTPUT" | grep -c "Warning:" || echo "0")
          if [ "$WARNING_COUNT" -gt 0 ]; then
            echo "❌ Build has $WARNING_COUNT warnings"
            echo "$BUILD_OUTPUT" | grep "Warning:"
            exit 1
          fi
          echo "✅ Build passed with 0 warnings"
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://placeholder.supabase.co' }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY || 'placeholder-anon-key' }}
          NEXT_PUBLIC_APP_URL: 'https://aiohub.jp'
          STRIPE_SECRET_KEY: ${{ secrets.STRIPE_SECRET_KEY || 'sk_test_placeholder' }}
          STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET || 'whsec_placeholder' }}

      - name: ✅ Quality Gate Passed
        run: echo "All quality checks passed!"
